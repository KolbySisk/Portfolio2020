import * as ScrollingDesignsStyles from './scrolling-designs.styles';
import { Color } from '../../styles';

const ScrollingDesigns = ({ children, backgroundColor }) => {
  return <ScrollingDesignsStyles.Root></ScrollingDesignsStyles.Root>;
};

interface Props {
  children: any;
  backgroundColor: Color;
}

export default ScrollingDesigns;
